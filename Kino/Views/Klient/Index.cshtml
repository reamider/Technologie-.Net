@model IEnumerable<Kino.Models.Klient>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Bilet.rodzaj)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.imie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nazwisko)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.data_zak)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Bilet.rodzaj)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.imie)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nazwisko)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.data_zak)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.klient_id }) |
                @Html.ActionLink("Details", "Details", new { id=item.klient_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.klient_id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
